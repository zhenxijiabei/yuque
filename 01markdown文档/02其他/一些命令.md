### sublime快捷键

快速关闭sublime多窗口
Alt+ Shift + 1

### 正则

正则匹配空行
\s+$

正则匹配行首数字字符
^\d.

### docker

初始: 解决了获取不到ip地址问题
https://www.cnblogs.com/tim-eff/p/8962293.html

解决docker爬去镜像太慢问题
https://www.cnblogs.com/spll/p/11828193.html

docker进入容器交互命令
docker exec -it 容器名 /bin/bash



### Linux命令

#### 查看当前目录下文件所占大小

du -x -h --max-depth=1 | sort -nr

du -x --max-depth=1 | sort -nr | head (从大到小排列，找出前10)

#### zip打包

将当前目录下cobaltstrike3.14文件夹所有内容打包成cobaltstrike3.14.zip

zip -q -r cobaltstrike3.14.zip ./cobaltstrike3.14

#### 使环境变量生效

source /etc/profile

#### 更新源


vim /etc/apt/sources.list

#中科大源
deb http://mirrors.ustc.edu.cn/kali kali-rolling main non-free contrib
deb-src http://mirrors.ustc.edu.cn/kali kali-rolling main non-free contrib



### python开启http服务

Python >= 2.4
python -m SimpleHTTPServer 8000

### jdk1.7

jdk1.7下载地址
https://www.oracle.com/java/technologies/javase/javase7-archive-downloads.html



### 网页合并多页为一页

```html
#起始一行
<form action="https://yqx.qianxin-inc.cn/m...

#终止后面一行
<div class="submitbtns"></div></div></form><div class="submit
```



### nmap

Nmap进行完整全面的扫描
nmap –T4 –A –v



### 防火墙开启/关闭

netsh advfirewall set allprofiles state off



centos

```shell
1、开放端口

firewall-cmd --zone=public --add-port=5672/tcp --permanent   # 开放5672端口

firewall-cmd --zone=public --remove-port=5672/tcp --permanent  #关闭5672端口

firewall-cmd --reload   # 配置立即生效

 

2、查看防火墙所有开放的端口

firewall-cmd --zone=public --list-ports

 

3、关闭防火墙

如果要开放的端口太多，嫌麻烦，可以关闭防火墙，安全性自行评估

systemctl stop firewalld.service

 

4、查看防火墙状态

 firewall-cmd --state

 

5、查看监听的端口

netstat -lnpt
```





### 名词解释

kdc
密钥分发中心

fqdn
完全限定域名





### python3安装包

更新pip 

```
python3 -m pip install --upgrade pip
```



通过pip安装包

```
python3 -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

```
pip install -i some-package https://pypi.tuna.tsinghua.edu.cn/simple
```



### 安装apache服务

```shell
rpm -qa  httpd		#查询所有已安装的软件包
yum list | grep httpd 	#列出yum库里面apache包

----
yum install httpd		#安装apache
Y
systemctl status httpd	#查看apache状态

systemctl start httpd	#开启apache服务

rpm -ql httpd | grep www	#查看释放路径
/var/www/html

systemctl status firewalld	#查看防火墙状态

systemctl stop  firewalld	#关闭防火墙
systemctl restart network	#重启网卡
```

